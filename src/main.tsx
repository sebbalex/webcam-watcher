import React from 'react'
import ReactDOM from 'react-dom/client'
import Carousel from './components/GalleryBox'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <Carousel />
  </React.StrictMode>
)
